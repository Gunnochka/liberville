# Liberville — сайт ЖК

Статичний сайт житлового кварталу **Liberville** (Ходосівка, Обухівський р-н, Київська обл.).
Забудовник — **Time Development**.

- **Код:** `/Users/gannochka/biorytm-site`
- **Прод:** https://liberville.com.ua
- **Репозиторій:** GitHub Pages, `Gunnochka/liberville`, гілка `main`
- **Деплой:** `git push` → 1–2 хв (іноді до 11). Жодної збірки немає.

---

## Як тут працювати

**Без збірки.** Чистий HTML + CSS + JS. Редагуєш файл — він і є продом після пушу.
Не додавати npm/бандлери без прямого прохання.

**Мова інтерфейсу — українська.** Уся видима користувачу текстівка, коміти, коментарі в коді.
Спілкування з власником — російською, простими словами (він не програміст).

**Перевіряти в браузері, а не на око.** Playwright MCP:
- Скриншоти в цьому оточенні **не зберігаються на диск** — не покладатись на них.
  Перевіряти через `browser_evaluate` + `getBoundingClientRect()` / `getComputedStyle()`.
- Скриншот або evaluate можуть відвалитись по таймауту на важких сторінках — глушити
  анімації (`* { animation: none !important }`) і ставити відео на паузу.
- Локальний сервер: `python3 -m http.server 8899` з кореня проєкту, ходити на `127.0.0.1`.

**Python: два різні.** `python3` → homebrew 3.14 (порожній).
**PIL, PyMuPDF, google-genai стоять у `/usr/bin/python3` (3.9)** — для роботи з
картинками, PDF і Vertex викликати саме його.

**Перед комітом:** `node --check js/main.js` і звірка балансу дужок у CSS
(`s.count('{') == s.count('}')`).

---

## Структура

```
index.html            головна (усі секції)
articles.html         перелік статей   article-*.html  5 статей
gallery.html  progress.html  news.html  privacy.html  404.html
css/style.css         увесь стиль одним файлом
js/main.js            уся логіка одним файлом
assets/data/plans.js  14 квартир — джерело правди для каруселі планувань
assets/img/plans/     kv-01…kv-22.webp/jpg — мебльовані планування
assets/img/infra/     обʼєкти інфраструктури — усі 12 закриті
assets/img/gp/        gp1…gp5 — кадри скрол-галереї генплану
assets/img/pattern-*.svg  фірмовий патерн (arcs + grid), підложка секцій
assets/video/         liberville-promo.mp4 (4K, 66 МБ) + -1080.mp4 (11 МБ)
```

---

## Ключові вузли

### Заявки — 4 канали одночасно
Конфіг `LEAD` на початку `js/main.js`: Telegram Bot API, Web3Forms, G-PLUS CRM,
плюс подія `generate_lead` у GA4 і `fbq('track','Lead')`.
Усі чотири перевірені живими заявками.

> ⚠️ **Ніколи не переписувати токени з картинок.** Одного разу через це в CRM
> потрапила цифра `0` замість літери `O` — і заявки мовчки не доходили кілька днів.
> Просити текстом.

### Карта інфраструктури (`.mapx`)
- **Десктоп:** секція заввишки 13 екранів, кадр «прилипає», обʼєкти зʼявляються по черзі.
- **Мобільний:** горизонтальна панорама + міні-карта з рамкою + стрілки.
  Ширину полотна рахує **CSS через `aspect-ratio`**, а не JS.
  > ⚠️ Раніше ширину рахував JS від `clientHeight` — Safari на iOS повертав її
  > невірно й лишав чорні поля по краях. Не повертатись до вимірювань у JS.
- Тап по мітці відкриває оверлей `.objx` — зум у точку генплану + опис.

### Карусель планувань (`.pcar`)
Дані з `assets/data/plans.js`, фільтр за кімнатністю, центральна картка велика,
сусідні відходять углиб. Планування витягнуті з робочої документації забудовника
(`upd-part.pdf`) — креслення з розмірами відрізані, лишились тільки мебльовані плани.

### Промо-фільм
Запуск **тільки по кліку** (`preload="none"`). За замовчуванням 1080p,
на великих екранах JS підміняє на 4K. У режимі економії трафіку — завжди 1080p.

### Блоки-тези (`.stmt`)
На мобільному текст **білий і внизу** в обох кадрах.
> ⚠️ Мобільні кадри цих блоків темні по всій висоті (26–42% яскравості) —
> темний текст там не читається ніде, скільки не совай. Перевірено вимірюванням.

---

## Чого не робити

- **Не вигадувати факти.** Демо-дані про хід будівництва вже одного разу висіли
  на проді як правда. Якщо реальних даних немає — писати «за запитом», не вигадувати.
- **Не перетискати відео** без прямого дозволу. Власник цінує максимальну якість.
  (Легку версію для мобільних він схвалив окремо — оригінал при цьому не чіпали.)
- **Не згадувати архітектурне бюро** — замовник просив прибрати з сайту.
- **Не ставити емодзі як іконки.** Тільки лінійні SVG у бронзі `--accent: #C0824E`.
- **Не лишати посилань у нікуди** (`href="#"`).

---

## Фірмовий стиль

- Шрифт **RoadUI** (4 накреслення, `assets/fonts/`), запасний Geologica.
- Бронза `--accent: #C0824E`, темний фон `--bg: #17120F`.
- Логотип — кінь + LIBERVILLE у бронзовій текстурі, преloader з мідним градієнтом.
- Джерело — офіційний логобук BRAINTANK.
- **Патерн** — дуги, кола й сітка бронзою. Клас `.has-pattern` + `--left`/`--right`,
  прозорість `.20`.
  > ⚠️ `background-position: left -230px center` — **невалідний CSS** (три значення).
  > Браузер викидає все правило, і патерн тихо зникає. Тільки 4-значний синтаксис:
  > `left -170px top 50%`.

---

## Аналітика й пошук

- **GA4** `G-SY2NJYV4XC` — на всіх сторінках. Події: `generate_lead`, `video_start`.
- **Search Console** — підтверджено через GA4, sitemap подано (10 сторінок).
- **Meta Pixel** — код готовий, потрібен вхід власника у Facebook.
- **Schema.org** на головній: Organization, WebSite, ApartmentComplex,
  14 × Apartment з реальними площами, VideoObject.

---

## Генерація картинок — Vertex AI

Робочий рецепт у `~/liberville-tools/gen.py`. Викликати `/usr/bin/python3`.

```python
PROJECT  = 'liberville-visuals-260826'
LOCATION = 'global'          # не us-central1
MODEL    = 'gemini-3-pro-image'
```

> ⚠️ **Imagen 3 і 4 віддають 404** у свіжому GCP-проєкті — відома проблема, не наша.
> Gemini-моделі при цьому працюють. Не витрачати час на діагностику Imagen.
> Ключ Gemini API — **окремий гаманець** від хмарних кредитів, там свій ліміт.
> На 429 — ретрай із паузою 20 с.

---

## Стан на 2026-08-28

**Готово:** CRM, SEO-основа, GA4, Search Console, фірмовий стиль, головний екран,
блоки-тези, інтерактивний генплан (десктоп + мобільна панорама), переходи до обʼєктів,
карусель 14 планувань, промо-фільм у двох якостях, 5 статей, розмітка квартир,
фірмовий патерн по сайту, скрол-галерея генплану замість білої схеми,
відео паркінгу (Kling, ватермарку зрізано), усі 12 обʼєктів інфраструктури,
рух неба на головній при прокрутці.

**Відкрито — чекає на власника:**
1. **Ціни квартир** → у картки каруселі (обіцяв надіслати).
2. **Реальні етапи будівництва** + фото з майданчика → повернути таймлайн на `progress.html`.
3. **Meta Pixel** — вхід у Facebook.
4. **Фінальний генплан** — замовник просив у правках замінити на фінальну версію
   й на 3D-візуалізацію, але файлів не надіслав.

**Правки з PDF — відповіді замовника отримано 2026-08-29:**
1. ✅ **Школа буде.** Спершу власник сказав, що школи не буде (комміт `44b7687` прибрав
   усі згадки) — потім замовник підтвердив зворотне, і школу повернуто.
   > ⚠️ **Не прибирати школу знову.** Це вже мінялось двічі. Мітка на генплані одна —
   > «Школа та садок», обʼєкт `data-i="8"`, фото `assets/img/infra/school.*`.
2. ✅ **Відстані вірні:** 5 км до Києва, 20 хв до центру. Обидві лишаються.
3. ⏳ **3D-генплан** — замовник уточнює, коли надішле оновлений файл.
4. ⏳ **Тераси** — фактично тераc немає в КВ-01…КВ-04 (2 поверх). Формулювання
   з правок — «Кожна квартира — з власною терасою». Питання власнику уточнюється.

**⚠️ Невирішене:** у промо-фільмі на останньому кадрі телефон **093 460 19 19**,
а на сайті скрізь **+380 77 507 55 57**. Власника попереджено двічі, рішення за ним.

**⚠️ Безпека:** у `~/.claude/settings.json` лежав GitHub-токен відкритим текстом.
Замінено на `${GITHUB_PERSONAL_ACCESS_TOKEN}`, але **сам токен ще не відкликано** —
`github.com/settings/tokens`. Також раніше засвітився пароль від CRM.
Не зберігати паролі в репозиторії.

**⚠️ Невирішене:** у промо-фільмі на останньому кадрі телефон **093 460 19 19**,
а на сайті скрізь **+380 77 507 55 57**. Власника попереджено, рішення за ним.

**⚠️ Безпека:** у попередній сесії випадково засвітився пароль від CRM —
власнику рекомендовано змінити. Не зберігати паролі ніде в репозиторії.
